[build]
  command   = "npm install && npm run build"
  publish   = "dist"
  base      = "apps/web"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
